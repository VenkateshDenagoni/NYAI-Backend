[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/app/start.sh"
numReplicas = 1

[build.env]
PYTHON_VERSION = "3.11.6"
USE_CHROMADB = "false"
LOG_TO_CONSOLE = "true" 